echo "# appaboutme" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/rakcomputing/appaboutme.git
git push -u origin main

1-git add .
2-git commit -m "Your commit message"
3-git branch -M main
4-git push -u origin main